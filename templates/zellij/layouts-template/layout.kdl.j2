// Zellij Session Layout: {{ zellij_kdl_session_name }}
layout {
    tab_template name="shell-in-dir" {
        pane size=1 borderless=true {
            plugin location="zellij:tab-bar"
        }
    }
    {{ zellij_kdl_layout_info | join('\n') }}
}
session_name "{{ zellij_kdl_session_name }}"
attach_to_session true
